<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.frontend</groupId>
	<artifactId>hotel-frontend</artifactId>
	<version>1.0.0</version>
	<name>Hotel Management Frontend</name>
	<description>JavaFX Frontend for Hotel Management System</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<!-- Default profile (overridden by Maven profiles) -->
		<active.profile>server</active.profile>
	</properties>
	<dependencies>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!-- Spring Boot Starter -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>

		<!-- Spring Boot Web for RestTemplate -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- Spring Boot Data JPA -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<!-- MySQL Connector -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Spring Security for password encoding -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>

		<!-- Jackson for JSON processing -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-core</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-annotations</artifactId>
		</dependency>
		
		<!-- Spring Boot Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- JavaFX Dependencies -->
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-controls</artifactId>
			<version>19.0.2.1</version>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-graphics</artifactId>
			<version>19.0.2.1</version>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-fxml</artifactId>
			<version>19.0.2.1</version>
		</dependency>
		<!-- https://mvnrepository.com/artifact/de.jensd/fontawesomefx -->
		<dependency>
			<groupId>de.jensd</groupId>
			<artifactId>fontawesomefx</artifactId>
			<version>8.2</version>
		</dependency>
		<dependency>
			<groupId>org.controlsfx</groupId>
			<artifactId>controlsfx</artifactId>
			<version>11.1.0</version>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.20</version>
			<scope>provided</scope>
		</dependency>
		<!-- https://mvnrepository.com/artifact/com.itextpdf/itextpdf -->
		<dependency>
			<groupId>com.itextpdf</groupId>
			<artifactId>itextpdf</artifactId>
			<version>5.5.13.3</version>
		</dependency>

		<!-- Apache POI for Excel export -->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi</artifactId>
			<version>5.2.3</version>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>5.2.3</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.apache.pdfbox/pdfbox -->
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>pdfbox</artifactId>
			<version>2.0.24</version>
		</dependency>

		<!-- ZXing for QR Code generation -->
		<dependency>
			<groupId>com.google.zxing</groupId>
			<artifactId>core</artifactId>
			<version>3.5.2</version>
		</dependency>
		<dependency>
			<groupId>com.google.zxing</groupId>
			<artifactId>javase</artifactId>
			<version>3.5.2</version>
		</dependency>

		<!--  <dependency>
              <groupId>com.ankush</groupId>
              <artifactId>SuperShope</artifactId>
              <version>0.0.1-SNAPSHOT</version>
          </dependency>-->
		<!-- https://mvnrepository.com/artifact/com.jfoenix/jfoenix -->
		<dependency>
			<groupId>com.jfoenix</groupId>
			<artifactId>jfoenix</artifactId>
			<version>8.0.10</version>
		</dependency>

		<!-- MaterialFX - Modern Material Design library for JavaFX -->
		<dependency>
			<groupId>io.github.palexdev</groupId>
			<artifactId>materialfx</artifactId>
			<version>11.17.0</version>
		</dependency>

		<!-- SpringDoc OpenAPI with Swagger UI -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.3.0</version>
		</dependency>

		<!-- JWT (JSON Web Token) for mobile API authentication -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.3</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.3</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.3</version>
			<scope>runtime</scope>
		</dependency>

	</dependencies>

	<build>
		<!-- Enable resource filtering to inject profile into application.properties -->
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>application.properties</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
				<excludes>
					<exclude>application.properties</exclude>
				</excludes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>com.frontend.Main</mainClass>
					<jvmArguments>
						--module-path ${project.build.directory}/javafx-sdk/lib
						--add-modules javafx.controls,javafx.fxml,javafx.graphics
					</jvmArguments>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.openjfx</groupId>
				<artifactId>javafx-maven-plugin</artifactId>
				<version>0.0.8</version>
				<configuration>
					<mainClass>com.frontend.Main</mainClass>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- ========================================== -->
	<!-- MAVEN PROFILES FOR SERVER AND CLIENT JARS -->
	<!-- ========================================== -->
	<!--
		AVAILABLE MAVEN COMMANDS:
		=========================

		BUILD JAR FILES:
		  mvn clean package -Pserver -DskipTests    (builds hotel-server-1.0.0.jar)
		  mvn clean package -Pclient -DskipTests    (builds hotel-client-1.0.0.jar)

		RUN APPLICATION DIRECTLY:
		  mvn spring-boot:run -Prun-server          (runs server mode)
		  mvn spring-boot:run -Prun-client          (runs client mode)

		BUILD EXE INSTALLERS:
		  mvn clean install -Pserver-exe -DskipTests  (builds HotelServer-1.0.0.exe)
		  mvn clean install -Pclient-exe -DskipTests  (builds HotelClient-1.0.0.exe)
	-->
	<profiles>
		<!-- ========================================== -->
		<!-- RUN PROFILES (Run directly without building JAR) -->
		<!-- ========================================== -->

		<!-- RUN SERVER: mvn spring-boot:run -Prun-server -->
		<profile>
			<id>run-server</id>
			<properties>
				<active.profile>server</active.profile>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
							<arguments>
								<argument>--spring.profiles.active=server</argument>
							</arguments>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- RUN CLIENT: mvn spring-boot:run -Prun-client -->
		<profile>
			<id>run-client</id>
			<properties>
				<active.profile>client</active.profile>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
							<arguments>
								<argument>--spring.profiles.active=client</argument>
							</arguments>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- ========================================== -->
		<!-- BUILD JAR PROFILES -->
		<!-- ========================================== -->

		<!-- SERVER PROFILE: mvn clean package -Pserver -DskipTests -->
		<profile>
			<id>server</id>
			<properties>
				<active.profile>server</active.profile>
			</properties>
			<build>
				<finalName>hotel-server-${project.version}</finalName>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
							<arguments>
								<argument>--spring.profiles.active=server</argument>
							</arguments>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- CLIENT PROFILE: mvn clean package -Pclient -DskipTests -->
		<profile>
			<id>client</id>
			<properties>
				<active.profile>client</active.profile>
			</properties>
			<build>
				<finalName>hotel-client-${project.version}</finalName>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
							<arguments>
								<argument>--spring.profiles.active=client</argument>
							</arguments>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- ========================================== -->
		<!-- JPACKAGE PROFILES FOR CREATING .EXE FILES -->
		<!-- ========================================== -->

		<!-- SERVER EXE: Build JAR first, then create installer -->
		<profile>
			<id>server-exe</id>
			<properties>
				<active.profile>server</active.profile>
			</properties>
			<build>
				<finalName>hotel-server-${project.version}</finalName>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.panteleyev</groupId>
						<artifactId>jpackage-maven-plugin</artifactId>
						<version>1.6.5</version>
						<configuration>
							<name>HotelServer</name>
							<appVersion>${project.version}</appVersion>
							<vendor>Hotel Management System</vendor>
							<description>Hotel Management Server - Desktop Application with REST API</description>
							<destination>${project.build.directory}/installer</destination>
							<input>${project.build.directory}</input>
							<mainJar>hotel-server-${project.version}.jar</mainJar>
							<mainClass>com.frontend.Main</mainClass>
							<javaOptions>
								<option>-Dspring.profiles.active=server</option>
								<option>-Dfile.encoding=UTF-8</option>
								<option>--add-opens=java.base/java.lang.reflect=ALL-UNNAMED</option>
								<option>--add-opens=java.base/java.lang=ALL-UNNAMED</option>
							</javaOptions>
							<type>EXE</type>
							<winDirChooser>true</winDirChooser>
							<winShortcut>true</winShortcut>
							<winMenu>true</winMenu>
							<winMenuGroup>Hotel Management</winMenuGroup>
							<winConsole>false</winConsole>
						</configuration>
						<executions>
							<execution>
								<id>jpackage-server</id>
								<phase>install</phase>
								<goals>
									<goal>jpackage</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- CLIENT EXE: Build JAR first, then create installer -->
		<profile>
			<id>client-exe</id>
			<properties>
				<active.profile>client</active.profile>
			</properties>
			<build>
				<finalName>hotel-client-${project.version}</finalName>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<mainClass>com.frontend.Main</mainClass>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.panteleyev</groupId>
						<artifactId>jpackage-maven-plugin</artifactId>
						<version>1.6.5</version>
						<configuration>
							<name>HotelClient</name>
							<appVersion>${project.version}</appVersion>
							<vendor>Hotel Management System</vendor>
							<description>Hotel Management Client - Desktop Application Only</description>
							<destination>${project.build.directory}/installer</destination>
							<input>${project.build.directory}</input>
							<mainJar>hotel-client-${project.version}.jar</mainJar>
							<mainClass>com.frontend.Main</mainClass>
							<javaOptions>
								<option>-Dspring.profiles.active=client</option>
								<option>-Dfile.encoding=UTF-8</option>
								<option>--add-opens=java.base/java.lang.reflect=ALL-UNNAMED</option>
								<option>--add-opens=java.base/java.lang=ALL-UNNAMED</option>
							</javaOptions>
							<type>EXE</type>
							<winDirChooser>true</winDirChooser>
							<winShortcut>true</winShortcut>
							<winMenu>true</winMenu>
							<winMenuGroup>Hotel Management</winMenuGroup>
							<winConsole>false</winConsole>
						</configuration>
						<executions>
							<execution>
								<id>jpackage-client</id>
								<phase>install</phase>
								<goals>
									<goal>jpackage</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
